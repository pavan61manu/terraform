terraform {
  backend "s3" {
    bucket         = "pavan-s3-demo" # change this
    key            = "pavan/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "terraform-lock"
  }
}
